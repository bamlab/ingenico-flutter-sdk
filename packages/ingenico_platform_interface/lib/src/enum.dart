enum Type {
  string,
  integer,
  numericstring,
  expirydate,
  boolean,
  date,
}

enum PreferredInputType {
  integerKeyboard,
  stringKeyboard,
  phoneNumberKeyboard,
  emailAddressKeyboard,
  dateKeyboard
}

enum ListType {
  text,
  list,
  currency,
  date,
  boolean,
}

enum PaymentProductFieldDisplayElementType {
  integer,
  string,
  currency,
  percentage,
  uri,
}

enum ValidationType {
  expirationDate,
  emailAdress,
  fixedList,
  iban,
  length,
  luhn,
  range,
  regularExpression,
  type,
  termsAndConditions,
}
